# ---------------------------------------------
# CodePush Server Environment Configuration
# ---------------------------------------------

# --- Server Settings ---
# Port the server will listen on
PORT=3000

# Base URL for downloading bundles (e.g. http://your-domain.com/download)
DOWNLOAD_URL=http://localhost:3000/download

# Security Token - MUST CHANGE FOR PRODUCTION!
# Used to sign JSON Web Tokens
JWT_TOKEN_SECRET=supers3cr3tt0k3n_changeme_please

# --- Database Settings (MySQL 5.7) ---
DB_HOST=db
DB_PORT=3306
DB_USER=codepush
DB_DATABASE=codepush

# Passwords - CHANGE THESE!
DB_PASSWORD=your_secure_password
MYSQL_ROOT_PASSWORD=your_root_password

# --- Redis Settings ---
REDIS_HOST=redis
REDIS_PORT=6379

# --- Storage Settings ---
# Base directory on your host machine to store data
# The containers will create subfolders here (e.g. /bundles, /mysql, /redis)
STORAGE_VOLUMES=/mnt/extra_storage/codepush
