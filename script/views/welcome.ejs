<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to the CodePush REST API!</title>
  <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <script src="https://unpkg.com/lucide@latest"></script>
  <style type="text/tailwindcss">
    @theme {
            --color-brand: #6366f1;
            --color-brand-dark: #4f46e5;
            --font-outfit: 'Outfit', sans-serif;
        }
        body {
            font-family: var(--font-outfit);
        }
    </style>
</head>

<body class="bg-[#020617] text-slate-100 min-h-screen flex items-center justify-center p-6 selection:bg-indigo-500/30">
  <!-- Animated Background -->
  <div class="fixed inset-0 overflow-hidden pointer-events-none">
    <div class="absolute -top-[10%] -left-[10%] w-[40%] h-[40%] bg-indigo-600/20 blur-[120px] rounded-full"></div>
    <div class="absolute -bottom-[10%] -right-[10%] w-[40%] h-[40%] bg-blue-600/10 blur-[120px] rounded-full"></div>
  </div>

  <main class="relative z-10 w-full max-w-lg text-center">
    <div
      class="bg-slate-900/50 backdrop-blur-2xl border border-white/10 rounded-3xl p-10 shadow-2xl relative overflow-hidden group">

      <div
        class="absolute inset-0 bg-gradient-to-br from-indigo-500/5 to-purple-500/5 opacity-0 group-hover:opacity-100 transition-opacity duration-500">
      </div>

      <div class="relative z-10">
        <div
          class="inline-flex items-center justify-center w-20 h-20 bg-gradient-to-tr from-indigo-500 to-purple-600 rounded-3xl shadow-xl shadow-indigo-500/20 mb-8 border border-white/10">
          <i data-lucide="server" class="text-white w-10 h-10" fill="currentColor"></i>
        </div>

        <h1 class="text-4xl font-extrabold tracking-tight text-white mb-4">
          Welcome to the CodePush REST API!
        </h1>

        <p class="text-slate-400 text-lg mb-10 leading-relaxed">
          You've successfully connected to your self-hosted CodePush server. Manage your apps, access keys, and
          deployments from the dashboard.
        </p>

        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <% if (isAuthenticated) { %>
            <a href="/dashboard"
              class="inline-flex items-center justify-center gap-2 bg-indigo-600 hover:bg-indigo-500 text-white font-semibold py-4 px-8 rounded-2xl transition-all shadow-lg shadow-indigo-500/30 active:scale-95">
              <i data-lucide="layout-dashboard" class="w-5 h-5"></i>
              Open Dashboard
            </a>
            <% } else { %>
              <a href="/auth/login"
                class="inline-flex items-center justify-center gap-2 bg-slate-800 hover:bg-slate-700 text-slate-300 font-semibold py-4 px-8 rounded-2xl border border-white/5 transition-all active:scale-95">
                <i data-lucide="log-in" class="w-5 h-5"></i>
                Sign In
              </a>
              Operational
              </span>
        </div>
      </div>
  </main>

  <script>
    lucide.createIcons();
  </script>
</body>

</html>